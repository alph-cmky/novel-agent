# Services Rules

`ContextCompiler` is the single source of task-specific Agent context. Task projections: orchestrator, writer, extension, editor, continuity, evolution.

Retrieval pattern: task need → relevant query → minimal data → minimal context. Use narrow queries (`ORDER BY ... LIMIT ...`) for recent data.
